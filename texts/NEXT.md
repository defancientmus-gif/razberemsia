# NEXT

Читать первым после `texts/SNAPSHOT.md`.

## Последнее важное

2026-05-31, `rz-v298` — синк починен, ошибок нет.

## Что сделано за сессию

- **Аудит и очистка кода** (v290–v291): −240 строк. Убраны двойной источник правды (notes table vs user_state), сложный folder merge → last-write-wins, Router V1 в двух местах → только клиент, fallback-костыли для старых колонок.
- **Правило**: не создавать костыли, переделывать правильно. Добавлено в `texts/RULES.md`.
- **Синк** (v292–v298): убран `_cloudStale`, SW больше не перехватывает кросс-доменные запросы (был возврат HTML вместо JSON), retry 800ms, Realtime с автопереподключением и задержкой 1.5с, `select('*')` вместо перечисления колонок.
- **Все Supabase миграции** подтверждены применёнными: `ai_memory`, `user_folders`, `tag_folders`, `user_state` в realtime publication.

## Следующие задачи

1. **Живой тест синка** на iPhone + Mac без VPN — убедиться что задержка < 2с в нормальной сети.
2. **Smoke-тест** роутера перед любой правкой агента: `node scripts/smoke-agent-router.mjs`.
3. **getTagFolders/saveTagFolders** в `index.html` — перенести в `app.js`, убрать кросс-файловую зависимость (не срочно, но это последний крупный дубль).
4. Если появятся новые баги синка — проверить консоль на `[rz:sync]` логи (они убраны, при необходимости вернуть).

Сделано:

- Исправлена кнопка `Сохранить`: `_reloadViews()` больше не вызывает саму себя.
- Поднят PWA cache `rz-v274` -> `rz-v275`.
- Закоммичено и отправлено в `main`: `f451e5b rz-v275: fix save sheet reload helper`.
- Создан локальный архив перед уборкой контекста: `backups/repo_context_before_cleanup_2026-05-29_after_v275.tar.gz`.
- В текущей уборке поднято до `rz-v276`, чтобы заменить старые ссылки `pwa-feather-*` на `pwa-logo-*`.
- Текущий фикс `rz-v277`: единый тег идеи `идея`, отправка `save_idea` из обычного `Сохранить`, нормализация AI-папок и защита от повторного клика по сохранению.
- Текущий UI-апскейл `rz-v278`: карточки/разделы/напоминания приведены к soft ceramic glass, AI-разбор всплывает по центру, `идея` подсвечивается мягким сиреневым, в превью видны теги, удаление заметки доступно свайпом в разделе и через меню `...` в открытой заметке.
- Текущий sync-фикс `rz-v279`: `Входящие` больше не зависят только от локального `tag_folders`, папки мержатся между устройствами, добавлены ручное обновление и pull-to-refresh в разделе заметок.
- Текущий legacy-фикс `rz-v280`: латинские теги `idei/ideya/ideja` тоже нормализуются в `идея`; старый unscoped `rz_tag_folders` переносится в scoped storage и пушится в облако.
- Текущий agent-фикс `rz-v281`: Router v1 делит запросы на `none/light/notes/deep`, поэтому простые команды больше не получают весь массив заметок, память, историю и статистику приложения.
- Текущий UX-фикс `rz-v282`: в шапку возвращен бейдж версии, а карточка агента открывает созданную заметку/напоминание по `id`, чтобы не попадать в старую заметку из-за задержки ленты.
- Текущий Router v1.1 `rz-v283`: синхронизированы клиент и Edge Function; добавлены формы `этом/этого/которую/него`, `расскажи/перечисли`, множественные `планы/расписание`, и `reminderNeedsNotes` для напоминаний со ссылкой на контекст заметок.
- Тестовая сетка закреплена: `texts/TEST_GRID.md`, авто-smoke `node scripts/smoke-agent-router.mjs`.

Почему это было критично:

- заметка сохранялась;
- затем UI падал на `_reloadViews()`;
- лист не закрывался;
- повторные клики по `Сохранить` создавали дубли.

## Что сейчас делаем

Делаем рабочие заметки, которыми приятно пользоваться без супер-агента:

1. Создать -> открыть именно эту заметку -> отредактировать -> сохранить без дублей.
2. Удалить -> корзина -> восстановить.
3. Найти заметку по заголовку, телу, тегу и разделу.
4. Быстро поставить/изменить/снять напоминание.
5. Mac/iPhone/Windows должны видеть одинаковые заметки, разделы и `Входящие`.
6. В шапке должен быть виден свежий `β rz-v...`.
7. AI пока усиливает заметки, но не является фундаментом: разобрать, предложить теги/раздел, найти, кратко объяснить.

## Следующие кодовые задачи

1. Пройти `texts/TEST_GRID.md` на живой PWA `rz-v283`.
   - Проверить `β rz-v283` в шапке.
   - Простая заметка: создать и открыть именно её из карточки агента.
   - Напоминание без лишних вопросов.
   - Поиск, удаление/восстановление.

2. После деплоя `rz-v283` провести живой тест `идея/idei` -> `Входящие` на Mac/iPhone/Windows.
   - Создать/открыть AI-папку через тег `идея`.
   - Проверить, что на другом устройстве она появляется как `Входящие` после live-pull, ручного обновления или перезагрузки.
   - Проверить, что счетчик совпадает с заметками с тегом `идея`.

3. Периодически проверять живой тест `идея` -> `save_idea` -> GitHub.
   - Локальный код нацелен на `texts/IDEAS_NEAR_TERM.md`.
   - Клиент вызывает `save_idea` и при AI-анализе, и при обычном `Сохранить`.
   - Edge Function `ai` задеплоена 2026-05-31 после Router v1.1.
   - Если не дошло: проверить `GITHUB_TOKEN`, auth-сессию пользователя и GitHub Contents API.

4. Разобрать legacy `ideas/`.
   - Старые raw-идеи 2026-05-19..22 перенесены в `texts/archive/raw-ideas-2026-05-23/`.
   - Новые raw-идеи 2026-05-25..28 еще лежат в `ideas/`.
   - Решить: оставить readonly или перенести/сжать в активные `IDEAS_*.md`.

5. Проверить Router v1.1 на живых голосовых командах.
   - Простые: `запиши купить молоко`, `запомни позвонить маме`.
   - Напоминания: `напомни купить фильтры`, `напоминай пить воду каждый день`.
   - Напоминания с контекстом: `напомни про то, что я хотел купить`, `напомни о ней завтра`.
   - Контекстные: `разбери эту заметку`, `прочитай последнюю`.
   - Широкие: `что у меня сегодня`, `составь планы по заметкам`.
   - Рискованные: `удали это`, `отмени напоминание`.
   - Норма: не больше одного уточняющего вопроса, только если без него можно сделать не то.

6. Проверить живую PWA после деплоя `rz-v283`.
   - Старые ссылки `pwa-feather-*` в активных `index.html` и `sw.js` заменены.
   - Если iOS все еще показывает старую иконку ярлыка, это отдельная переустановка ярлыка, не runtime-баг.

7. Разобраться с Realtime migration.
   - В `js/app.js` уже есть Supabase Realtime-подписка на `user_state` и fallback polling 8с.
   - Локально есть untracked `supabase/migrations/20260531_realtime_user_state.sql`.
   - Нужно решить: миграция уже применена/нужна в git или это черновик, который нельзя тащить в релиз.

## Продуктовый фокус

Мы на этапе надежности.

Цель: пользователь верит, что агент помнит, раскладывает и не теряет. Красота важна, но база должна быть предсказуемой.

Главная линия интерфейса:

- легкие парящие заметки;
- soft matte glass, керамика/хрусталь без дешевой зелени;
- видимая структура, не хаос;
- пожилой человек должен понять действие без обучения.

## Быстрый деплой, который нельзя сломать

Рабочая схема:

1. правка;
2. `node --check js/app.js`;
3. bump `CACHE` в `sw.js`;
4. commit/push;
5. через 1-2 минуты PWA видит обновление.

`sw.js` + `app.js` уже настроены на быстрый update. Не менять эту связку без отдельной причины.
